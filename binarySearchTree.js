export const BinarySearchTree = (arr) => {
  let root = null;

  const buildTree = (arr) => {
    return null;
  };

  return (Object.freeze({
    buildTree,
  }));
};

const result = BinarySearchTree();

console.log(result);
